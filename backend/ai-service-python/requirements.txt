fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
aiosqlite==0.20.0

# --- pick ONE LLM provider (uncomment what you use) ---
# anthropic==0.39.0 forwards a `proxies` kwarg to httpx that httpx>=0.28 no
# longer accepts, crashing every call with "unexpected keyword argument
# 'proxies'". >=0.50.0 dropped that passthrough — pin at least that.
anthropic>=0.50.0
# google-genai==0.8.0
# openai==1.51.0
